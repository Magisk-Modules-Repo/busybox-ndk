#!/sbin/sh
# Module Workaround Backend: Recovery Flashable Zip
# osm0sis @ xda-developers

# avoid forced Magisk update-binary replacement breakage
# see install.sh for what was previously update-binary

mkdir -p /dev/tmp;
cd /dev/tmp;

unzip -oj "$3" install.sh;

. /dev/tmp/install.sh;

